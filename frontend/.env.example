# Base URL of the backend API (no trailing slash).
# Local dev points at the Express server; in production set this to your
# deployed backend, e.g. https://url-shortener-api.onrender.com
VITE_API_URL=http://localhost:3000
