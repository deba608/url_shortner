# Faster npm installs on CI/CD
# prefer-offline: use cache before hitting registry
# audit=false: skip security audit during install (saves 10-20s)
# fund=false: skip funding messages
prefer-offline=true
audit=false
fund=false
